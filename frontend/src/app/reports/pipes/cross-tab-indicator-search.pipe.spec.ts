import { CrossTabIndicatorSearchPipe } from './cross-tab-indicator-search.pipe';

describe('CrossTabIndicatorSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new CrossTabIndicatorSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
