import { AreaFilterPipe } from './area-filter.pipe';

describe('AreaFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AreaFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
