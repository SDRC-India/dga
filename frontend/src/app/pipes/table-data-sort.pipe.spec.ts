import { TableDataSortPipe } from './table-data-sort.pipe';

describe('TableDataSortPipe', () => {
  it('create an instance', () => {
    const pipe = new TableDataSortPipe();
    expect(pipe).toBeTruthy();
  });
});
