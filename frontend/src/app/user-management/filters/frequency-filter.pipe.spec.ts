import { FrequencyFilterPipe } from './frequency-filter.pipe';

describe('FrequencyFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FrequencyFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
